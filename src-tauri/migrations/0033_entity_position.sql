-- Позиция таблицы на ERD-холсте, в мировых координатах сцены.
--
-- NULL означает «сущность ещё ни разу не размещали»: сцена разложит её
-- автолейаутом и сразу вернёт результат на сохранение, поэтому NULL живёт
-- только до первого открытия диаграммы. Так автолейаут остаётся одноразовым
-- решением, а не пересчитывается при каждой загрузке.
ALTER TABLE entities ADD COLUMN pos_x REAL;
ALTER TABLE entities ADD COLUMN pos_y REAL;
