-- У документа нет тегов.
--
-- doc_api_tag заполнялся из двух форм — создания и правки документа — и на
-- этом всё: ни списка по тегу, ни фильтра, ни показа на карточке документа.
-- Прочитанные теги доезжали до фронтенда и оседали в поле ввода, чтобы их
-- можно было записать обратно. Место документа задаёт дерево каталогов, и
-- вторая, ни на что не влияющая раскладка ему не нужна.
--
-- Теги эндпоинтов (endpoint_tag) остаются: по ним группируется список внутри
-- документа.
--
-- Файлы импорта старого формата с `doc.tags` читаются по-прежнему: лишний ключ
-- заголовка просто не доезжает до бэкенда.
DROP TABLE doc_api_tag;
