-- У эндпоинта нет тегов.
--
-- Вслед за тегами документа (0037) уходят и теги эндпоинта. Они показывались
-- строкой плашек в шапке страницы эндпоинта и правились в двух формах — и на
-- этом всё: ни поиска по тегу, ни фильтра, ни группировки. Место эндпоинта
-- задаёт группа внутри документа, а назначение — имя и описание.
--
-- Плашка «Auth required» остаётся: она рисуется по колонке `auth` эндпоинта,
-- а не по тегу.
--
-- Файлы импорта старого формата с `tags` у эндпоинта читаются по-прежнему:
-- лишний ключ просто не доезжает до базы.
DROP TABLE endpoint_tag;
