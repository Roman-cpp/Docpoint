-- У узла дерева нет описания.
--
-- Поле `desc` было у каждого узла — каталога и документа любого вида, — но
-- в интерфейсе оно жило только в формах создания и переименования, а видно
-- было разве что во всплывающей подсказке плитки. Описание документу даёт его
-- собственное тело: markdown — текст, doc-api — эндпоинты, диаграмма — таблицы.
-- Колонка уходит вместе с данными: ни индекс, ни ограничение на неё не
-- ссылаются, поэтому пересборка таблицы не нужна.
ALTER TABLE catalog_node DROP COLUMN desc;
