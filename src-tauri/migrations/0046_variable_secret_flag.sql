-- Раньше «секретность» переменной определялась только по имени (regex на
-- фронтенде) — переменная с непримечательным именем никак не маскировалась.
-- Явный флаг переопределяет эвристику.
ALTER TABLE variables
    ADD COLUMN is_secret INTEGER NOT NULL DEFAULT 0;
