/// Строка диаграммы. Собственных полей у неё пока нет — она существует ради
/// ссылки: сущности цепляются к диаграмме, а не к произвольному узлу дерева.
/// Имя и описание живут в узле, заводит и удаляет его репозиторий каталога.
pub trait DocErdRepository {
    async fn create(&self, id: &str) -> Result<(), String>;
}
