/** Файл, выбранный в системном диалоге, но ещё не загруженный: путь, по
 *  которому его заберёт бэкенд, и то, что показать до создания узла. */
export interface PickedFile {
	path: string;
	name: string;
	/** Размер в байтах. */
	size: number;
}
